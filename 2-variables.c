#include <stdio.h>

int main() {
    int firstNumber, secondNumber = 19;
    firstNumber = 10;
    printf("The age of this guy: %d", firstNumber);

    // int secondNumber = firstNumber;
    printf("The second number is %d", secondNumber);

    return 0;
}

//You cannot start a variable name with numbers
//You cannot add spaces inside variable names
//You cannot use certain names as variable names    